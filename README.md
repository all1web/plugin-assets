# plugin-assets

Public marketing assets for ALL 1 NativePHP plugins (listing images only —
no source, no docs). Referenced by absolute URL from plugin READMEs so the
marketplace and GitHub both render them.
